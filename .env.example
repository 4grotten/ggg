# Copy this file to .env and fill real secrets for local development
# Minimal required values to run the stack (dev-only placeholders):
JWT_SECRET=changeme_jwt_secret
ANON_KEY=changeme_anon_key
SERVICE_KEY=changeme_service_role_key
LOVABLE_API_KEY=changeme_lovable_key
ELEVENLABS_API_KEY=changeme_elevenlabs_key
# Postgres / Redis (only if you want to override defaults):
POSTGRES_USER=easycard
POSTGRES_PASSWORD=changeme_secure_password
POSTGRES_DB=easycard
REDIS_PASSWORD=redis_secure_password
# Optional external URLs
API_EXTERNAL_URL=http://localhost
SITE_URL=http://localhost
